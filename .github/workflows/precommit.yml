on:
  workflow_dispatch:
  push:
    paths-ignore:
      - 'changelog/config.env'

  pull_request:

jobs:
  run-linters:
    name: Run linters
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v2

      - name: Run linters
        uses: larsks/cached-go-precommit@v1
